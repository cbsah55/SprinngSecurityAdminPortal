server.servlet.context-path=/adminportal
server.port=8081
spring.thymeleaf.cache=false

#Data source
#set here the configuration of the database connection
spring.datasource.url=jdbc:mysql://localhost:3306/merodokan

#database secrets and passwords
spring.datasource.username=root
spring.datasource.password=ibmNhf1$
spring.datasource.driverClassName = com.mysql.cj.jdbc.Driver



# Specify the DBMS
spring.jpa.database = MYSQL

#keep the connection alive if idle for long time (neeeded in production)
spring.datasource.dbcp2.test-while-idle=true

spring.datasource.dbcp2.validation-query== SELECT 1 

#show or not log for each sql query
spring.jpa.show-sql=true

#if no tables exsts then automatically creates table in database
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
#allow hibernate to generate SQL optimized for a particular database

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect